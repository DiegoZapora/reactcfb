export default function Samp(props) {
    return (
        <section>
            <p>Nome: {props.nome}</p>
            <p>Album: {props.album}</p>
            <p>Musica: {props.musica}</p>
        </section>
    )
}